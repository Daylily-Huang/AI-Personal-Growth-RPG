# ── Demo mode ─────────────────────────────────────────────
# 不配置任何 key 时，应用使用本地 JSON 文件 + 确定性 mock AI 评估。
# 数据保存在 .data/demo.json。

# ── Optional: OpenAI-compatible AI (also works with local bridge) ──
# 例：本地 bridge
# AI_BASE_URL=http://127.0.0.1:3099/v1
# AI_API_KEY=dummy
# AI_MODEL=deepseek-v4-flash
#
# 例：OpenAI
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4.1-mini

# ── Supabase (optional real backend) ──────────────────────
# 仅配置 URL/key 并不会切换运行时。只有 API 层显式调用
# getAuthenticatedRepository() 时才使用用户 Session + RLS 的 SupabaseRepository；
# 当前 Dashboard/Quick Log 默认仍保持 DemoRepository，直到 Stage2-B settlement RPC 完成。
# 命名约定（2026-08-17 采用，替换旧 ANON_KEY / SERVICE_ROLE_KEY）：
#   NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY —— 公开给浏览器（等同 anon key），
#       只用于 Supabase Auth 起始 + RLS 约束下的用户自己数据的操作。
#   SUPABASE_SECRET_KEY —— 机密，仅限服务端管理员场景（绕过 RLS），
#       绝不可进浏览器或提交仓库。默认业务不使用它发请求。
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
# SUPABASE_SECRET_KEY=sb_secret_...
