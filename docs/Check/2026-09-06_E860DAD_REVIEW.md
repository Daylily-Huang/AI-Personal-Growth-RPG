# e860dad 修复复审 — 2026-09-06

审查对象：`feature/phase5-skills-modernization`，HEAD `e860dad90c07dd3685d230dd09c9f9faf6c922bd`。本地 main 和 origin/main 均为 a987092；未刷新远程 refs 或核验 GitHub CI。仅审查，未修改业务代码。

## 结论

此前报告的业务错误已在当前代码中得到修复，但本次提审仍不能判定为全部通过：界面冻结范围检查失败，真实 HTTP 验收环境尚未适配 AI 失败隔离，新增权威交接文档有规则误述。

## 已关闭的代码问题

- **掌握度跨技能/跨实体误授**：两种原始隔离复现均从 upgrade 变为 none，新增回归通过。
- **新技能空 UUID 查询**：服务与 SupabaseRepository 均有空 ID 短路。真实客户端配合 fetch 替身验证空 ID 返回 0、发送请求数为 0。
- **正式存储缺少 AI 配置时使用 mock**：默认调用现在拒绝并返回 ai_not_configured；真实路由仅在未配置 Supabase 时显式许可 Demo。显式 Demo 调用仍可用。
- **重复计数受返回行数上限截断**：改用 exact/head 计数；模拟 Content-Range 为 1501 的响应正确返回 1501。此为客户端与仓储行为验证，不是 1501 条真实数据库测试。
- **lint 错误**：新增测试中的 any 和未使用导入已修复，本轮 lint 通过。
- **缺失的交接主文档**：文件已经存在，README 启动规范入口也已更新；内容问题见下文。

## R1 [P1] 当前 UI 分支包含后端修复，触发冻结门禁

证据：本轮 pnpm test 的五个冻结检查失败，涉及 Artifact UI、AppShell、Dashboard、Skills、Quests。它们以主分支为基线检查是否修改 src/app/api、src/lib/store、src/lib/ai 等路径；e860dad 正好修改了这些路径。

位置示例：`tests/phase5-dashboard-ui.test.tsx:662–702`、`tests/stage7c-ui.test.tsx:1355–1398`、`tests/phase5-skills-ui.test.tsx:480`。

这证明当前提交组织不满足该 UI 分支约定，不代表业务修复应撤回。建议把后端修复作为独立、明确授权范围的修复变更验收，再为 UI PR 使用更新后的正确基线；若项目决定建立修复例外，需同步精确的范围规则。不要删除冻结断言或宽泛跳过门禁来制造全绿。

## R2 [P1] 真实 HTTP happy path 仍依赖被移除的隐式 mock

位置：`tests/e2e-http-browser.test.ts:157–164`、`tests/stage5d-skills-http.test.ts:85–87,135`，以及 `.github/workflows/ci.yml` 的 supabase-integration job。

实际 HTTP 测试启动生产 Next 服务器，使用真实 Supabase，并直接调用 assess 后断言 200；Skills 用例明确依赖旧 fallback 的关键词映射。当前 CI 只配置 Supabase，没有配置 AI 测试服务。新代码在该条件下正确返回 ai_not_configured/502，所以这些 happy path 已失去输入依赖。

这是从 CI 配置和真实路由代码推导的确定触发条件；本轮因缺少专用测试数据库而跳过 live HTTP 套件，未声称现场跑出了这些失败，也未核验远程 CI 状态。

建议为集成测试提供隔离的确定性 OpenAI-compatible HTTP 测试服务，或由可信测试 setup 构造提议以验证后续结算；同时保留独立 assess 路由成功/失败测试。不得重新放开生产隐式 mock。补齐后需运行真实数据库/HTTP 门禁。

## R3 [P2] 权威交接文档误述 Mastery 范围与验证门槛

位置：`docs/MASTER_PROJECT_HANDOFF.md:35–36`。

新文档写 Mastery Level 0~5、M3 及以上必须 Verified Evidence；上位规则 §7 定义 M0–M10，当前引擎 `src/lib/growth-engine/mastery.ts:51` 的晋级验证条件是目标 >=5 或跨级 >=2。引擎还单独按证据等级约束上限。

既然该文档被 AGENTS 指为第一个入口，错误规则会误导后续实现。建议纠正文档并引用原始规范，不修改引擎迎合误述。

**对上一轮判断的纠正**：a987092 是本地主分支基线，e860dad 是功能分支 HEAD，两者不同本身不是文档错误。上一轮把 HEAD 与 main 混同后得出的“main 基线过期”判断撤回。远程最新状态仍未核验。

## 验证范围

- lint、TypeScript 检查通过。
- WSL 生产构建通过，退出码 0。
- 空 ID、精确计数 1501、AI 默认拒绝/显式 Demo、两种 Mastery 错配均完成隔离验证，无真实用户数据写入。
- 全量测试最终计数见下方追加记录。
- 真实 PostgreSQL/RLS/HTTP、真实模型质量、浏览器视觉与远程 CI 本轮未验证。

复现脚本及运行日志：`.planning/2026-09-05-project-review/final-probe.ts`、`probe.ts`、`2026-09-06-tests.log`、`2026-09-06-build.log`。历史 09-05 报告保留用于追溯，当前判断以本报告为准。

### 全量测试最终结果

- 56 个文件：32 passed / 5 failed / 19 skipped。
- 900 个用例：616 passed / 5 failed / 279 skipped，进程退出码 1。
- 五个失败均为 R1 的冻结范围检查；Growth Engine 11/11 通过。
- 279 项数据库/HTTP 等门控测试跳过，不能列作通过。
